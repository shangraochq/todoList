{
  "name": "todolist",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "npm install && node --max-old-space-size=380 ./bin/www",
    "build": "cd ./clientApp && npm install && npm run build",
    "pull": "git pull origin master",
    "publish": "npm run pull && npm run build && npm run start",
    "restart": "node ./bin/www"
  },
  "dependencies": {
    "cookie-parser": "~1.4.3",
    "debug": "~2.6.9",
    "ejs": "~2.5.7",
    "express": "~4.16.0",
    "http-errors": "~1.6.2",
    "morgan": "~1.9.0",
    "express-session": "*",
    "csurf": "*",
    "compression": "*",
    "bcryptjs": "*"
  }
}
